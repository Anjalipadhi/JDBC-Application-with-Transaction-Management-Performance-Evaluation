# JDBC-Application-with-Transaction-Management-Performance-Evaluation
A JDBC-based Library Loan Management System using Apache Derby with transaction management, CRUD operations, batch processing, and performance benchmarking. Implements commit, rollback, savepoints, and ACID properties for efficient database handling.
